# Base runtime: what a validator needs, and nothing more.
#
# A validator reconstructs nothing and scores nothing, so the tensor stack is
# not here. Install the extra for your role instead:
#
#   validator   pip install -e .
#   miner       pip install -e ".[miner]"
#   engine      pip install -e ".[backend]"
#   pool import pip install -e ".[registry]"
bittensor>=11.0.0,<12
pydantic>=2.6
httpx>=0.27
jsonschema>=4.21
PyYAML>=6.0
rich>=13.0
